# Thesis Figure Manifest — Core Set

Every figure below defends exactly one thesis claim. Files live in
`reports/figures/thesis/` (PNG for Word, PDF for LaTeX); each is regenerated by
its notebook via `make notebooks`. Everything NOT in this manifest is
supporting material: use in appendices or omit.

## The problem (Chapter 1 / Results intro)

| Figure | Claim defended | Producer |
|---|---|---|
| `fig_03_monthly_cancel_rate_trend` | Cancellation is large (37%) and time-varying → chronological evaluation is mandatory | NB 01 |
| `fig_04_feature_distributions` | Booking-time features separate cancellers (lead time ~130 vs ~45 days) → prediction is feasible | NB 01 |

## Model selection & H2 (GBTs vs baselines)

| Figure | Claim defended | Producer |
|---|---|---|
| `fig_02_grouped_bar_model_selection` | LightGBM wins the prespecified rolling-origin validation protocol | NB 02 |
| `fig_7.6_paired_delta_forest` | Honest H2: champion-vs-baseline deltas with 95% CIs (GBT-vs-LR gap is marginal) | NB 07 |
| `fig_7.8_champion_split_decision` | **NEW** — LightGBM/XGBoost split decision at matched capacity: all CIs hug zero → protocol + efficiency decide | NB 07 |
| `fig_7.5_speed_vs_accuracy_pareto` | LightGBM trains 2.8× faster at equal quality (the rational tiebreaker) | NB 07 |

## Core performance

| Figure | Claim defended | Producer |
|---|---|---|
| `fig_01_roc_pr_curves` | Headline discrimination: ROC-AUC 0.863 / PR-AUC 0.759 on untouched test data | NB 02 |
| `fig_05_calibration_reliability_and_histogram` | Probabilities are trustworthy: ECE 0.062 → 0.031 after isotonic calibration | NB 03 |
| `fig_03_normalized_confusion_matrix_max_f1` | What the deployed operating point does to real bookings | NB 02 |

## Business value & H4 (cost-sensitive thresholding)

| Figure | Claim defended | Producer |
|---|---|---|
| `fig_30_policy_cost_ladder_test` | **NEW** — de-circularised H4 on the test set: the model beats even intervene-on-everyone by ~36% | NB 06 |
| `fig_103_threshold_policy_comparison` | The three threshold policies as one precision/recall/cost trade-off | NB 10 |
| `fig_101_cost_sensitivity` | The cost policy is robust to the €15 FP-cost assumption | NB 10 |

## Explainability (H1 / H3)

| Figure | Claim defended | Producer |
|---|---|---|
| `fig_13_shap_feature_importance_bar` | Global drivers: deposit type, lead time, prior cancellations (H1/H3 verdict) | NB 05 |
| `fig_14_shap_beeswarm` | Direction and spread of every driver's effect | NB 05 |
| `fig_16_shap_waterfall_examples` | Single-booking explanations — the bridge to the live app | NB 05 |

## Validity & robustness

| Figure | Claim defended | Producer |
|---|---|---|
| `fig_22_feature_ablation_deltas` | Dependence on the deposit-type artifact is quantified, not hidden | NB 03 |
| `fig_21_expanding_window_cv` | Performance is stable across time windows (no lucky split) | NB 03 |

## Transferability (PH pilot)

| Figure | Claim defended | Producer |
|---|---|---|
| `fig_112_transferability_rank_slope` | **NEW** — algorithm rankings transfer across markets (ρ=0.71); GBTs top both | NB 11 |
| `fig_111_transferability_cv_spread` | Why pilot metrics are directional: fold spread at n≈19 vs n≈9.5k | NB 11 |

## Methodology / system chapters (regenerable diagrams)

`fig_conceptual_systems_positioning`, `fig_deployment_framework`
(producers: `scripts/create_*_diagram.py` on the master lineage).

## Explicitly NOT for the main text

- `fig_02_cancellation_rate_donut` — a single number dressed as a chart; write "37%" instead
- `fig_7.7_multi_criteria_parallel` — parallel coordinates are unreadable for non-technical panels
- The deep ADR series `fig_41–51` — keep `fig_44/45/46` in the ADR section, rest to appendix
- Per-segment drill-downs (`fig_17–20` segment variants, `fig_25–29`) — appendix
- Monitoring series `fig_81–85` — one figure suffices to show the template exists
