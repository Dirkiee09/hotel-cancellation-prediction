bandit==1.8.3
detect-secrets==1.5.0
fastapi==0.128.0
gradio==6.7.0
# Transitive security pins (patched CVEs) — exact-pinned so the CI
# `pip-audit -r requirements.txt --no-deps` step accepts them. The
# >= form was rejected by pip-audit with: "ERROR: requirement X is
# not pinned to an exact version".
idna==3.17
ipykernel==7.2.0
joblib==1.5.3
jupyterlab==4.5.7
lightgbm==4.6.0
matplotlib==3.10.8
mypy==1.15.0
nbconvert==7.17.1
nbformat==5.10.4
numpy==2.3.5
optuna==4.2.0
pandas==2.3.3
pip-audit==2.7.3
pre-commit==4.0.1
pydantic==2.12.5
pytest==9.0.3
pytest-cov==6.0.0
# Transitive security pin (CVE patched):
python-multipart==0.0.29
# Transitive security pin (CVE patched):
requests==2.34.2
ruff==0.6.8
scikit-learn==1.8.0
scipy==1.17.0
seaborn==0.13.2
shap==0.51.0
# starlette pinned to 0.50.x to match fastapi 0.128 + gradio 6.7
# (CVE PYSEC-2026-161 needs starlette >= 1.0.1, but FastAPI ecosystem
# doesn't support starlette 1.x yet — known upstream limitation).
statsmodels==0.14.6
# Transitive security pin (CVE patched):
urllib3==2.7.0
uvicorn==0.40.0
xgboost==3.1.3
